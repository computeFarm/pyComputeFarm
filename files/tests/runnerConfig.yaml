# This is the task worker description of the runner command

# How to contact the TaskManager
#taskManager:
#  host: localhost
#  port: 8888

workerType: runner

cmd:
  prefix: ./runner

# env:
#  silly: sillier

# dir: aPath

verbose: true
