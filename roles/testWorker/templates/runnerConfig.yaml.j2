# This is the task worker description of the {{ item }} runner command

# How to contact the TaskManager
taskManager:
  host: {{ taskMaster.host }}
  port: {{ taskMaster.port}}

logParser: runnerLogParser-{{item}}
workerType: runner

cmd:
  prefix: ./runner-{{item}}

# env:
#  silly: sillier

# dir: aPath

verbose: true
