
# This is the log parser for the simple testWorker - bash runner script

import time

def logParser(logName, logMsg) :
  return {
    'time'  : time.time(),
  	'name'  : logName,
  	'level' : 'info',
  	'msg'   : logMsg

  }
