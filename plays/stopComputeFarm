---
# This playbook STOPS a compute farm

- name: Stop the compute farm on the remote servers first
  hosts: all:!localhost
  roles:
    - stopCF

- name: Now stop the compute farm on the local server
  hosts: localhost
  connection: local
  roles:
    - stopCF

