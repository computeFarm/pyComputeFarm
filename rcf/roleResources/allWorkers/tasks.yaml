targetDirs:
  - "{sysHome}"
  - "{pcfHome}/bin"
  - "{pcfHome}/files"
  - "{pcfHome}/lib"
  - "{pcfHome}/tmp"
  - "{pcfHome}/config"

files:
  - src: worker
    dest: "{pcfHome}/bin/worker"
    mode: 0755
